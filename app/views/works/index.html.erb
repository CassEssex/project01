<h1> The works </h1>

<% @works.each do |work| %>
 <%= image_tag(work.image, :class => 'thumb') %>
 
   <ul>
    <li><%= link_to work.title, work_path(work.id) %></li>
    <li><%= work.year %></li>
    <li><%= work.medium %></li>
    <li><%= work.style %></li>
   </ul>

<% end %>
